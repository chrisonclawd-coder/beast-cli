/**
 * Beast CLI — Hooks Module
 * 
 * Execute hooks on various events.
 */

export { HookExecutor, createHookExecutor, getHookExecutor } from "./executor"
export type { HookExecutorConfig, HookResult } from "./executor"
export * from "./types"
