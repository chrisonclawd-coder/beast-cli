/**
 * Beast CLI — Web Search Module
 * 
 * Web search with caching support.
 */

export { 
  WebSearch, 
  DuckDuckGoEngine, 
  MockSearchEngine, 
  createWebSearch, 
  getWebSearch 
} from "./interface"
export type { WebSearchResult, WebSearchOptions, WebSearchEngine } from "./interface"
