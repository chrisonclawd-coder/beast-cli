/**
 * Beast CLI — Config Index
 */

export { ConfigManager, getConfig, setConfig, type BeastConfig } from "./manager"

// Re-export zod schema for validation
export { BeastConfigSchema } from "./manager"
